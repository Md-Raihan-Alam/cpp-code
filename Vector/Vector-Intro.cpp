#include<iostream>
#include<vector>
#include<conio.h>
int main()
{
	std::vector<int> data;
	//std::vector<int> cv[5];
	//std::vector<int> cv(5,'x); initiallize a 5-elements char vector
	//std::vector<int> iv2(iv);;//creates an int vector from an int vector
	data.push_back(12);
	std::cout<<data[0];
	getch();
}
