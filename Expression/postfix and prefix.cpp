#include<iostream>
#include<conio.h>
int main()
{
	int i=0;
	i++;//first the value will print than increase
	++i;//first the increase than value will print
	i--;//first the value will print than decrease
	++i;//first the decrease than value will print

	getch();
}
